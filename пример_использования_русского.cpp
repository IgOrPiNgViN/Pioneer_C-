/*
===========================================
ПРИМЕР ИСПОЛЬЗОВАНИЯ РУССКОГО ЯЗЫКА В C++
===========================================

Этот пример показывает, как использовать заголовочный файл
russian_support.h для корректного отображения русских символов
в консоли Windows.
*/

#include <iostream>
#include "russian_support.h"  // Подключаем поддержку русского языка

using namespace std;

int main() {
    // ВАЖНО: Вызовите setupRussian() в самом начале программы!
    setupRussian();
    
    // Теперь можно использовать русские символы
    cout << "Привет, мир!" << endl;
    cout << "Это тест русского языка в C++" << endl;
    cout << "Работают все символы: А-Я, а-я, цифры 0123456789" << endl;
    
    // Пример с вводом и выводом
    string name;
    cout << "\nВведите ваше имя: ";
    cin >> name;
    cout << "Здравствуйте, " << name << "!" << endl;
    
    // Пример с числами
    int число;
    cout << "\nВведите число: ";
    cin >> число;
    cout << "Вы ввели: " << число << endl;
    
    return 0;
}

/*
===========================================
АЛЬТЕРНАТИВНЫЙ СПОСОБ (без отдельного файла)
===========================================

Если вы не хотите создавать отдельный файл russian_support.h,
вы можете просто добавить этот код в начало вашей программы:

#include <iostream>
#ifdef _WIN32
    #include <windows.h>
#endif
#include <locale>

using namespace std;

int main() {
    // Настройка русского языка
    #ifdef _WIN32
        SetConsoleOutputCP(65001);
        SetConsoleCP(65001);
        setlocale(LC_ALL, "Russian");
    #else
        setlocale(LC_ALL, "ru_RU.UTF-8");
    #endif
    
    // Ваш код здесь
    cout << "Привет, мир!" << endl;
    
    return 0;
}

===========================================
НАСТРОЙКА В VISUAL STUDIO
===========================================

Если вы используете Visual Studio и русский текст всё ещё
отображается неправильно:

1. Сохраните исходный файл в кодировке UTF-8:
   - File -> Advanced Save Options
   - Выберите "Unicode (UTF-8 with signature) - Codepage 65001"

2. Или используйте параметры компилятора:
   - В настройках проекта добавьте: /utf-8

===========================================
*/








