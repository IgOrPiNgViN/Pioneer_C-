/*
===========================================
УРОК 2: ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ
===========================================

ЦЕЛИ УРОКА:
- Изучить основные типы данных в C++
- Научиться объявлять и инициализировать переменные
- Понять разницу между различными типами данных
- Изучить правила именования переменных
- Научиться работать с константами

ТЕОРЕТИЧЕСКАЯ ЧАСТЬ:

ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ:

Переменная - это именованная область памяти, которая хранит данные определенного типа.
Каждая переменная имеет имя, тип и значение. Тип определяет, какие данные можно хранить
и сколько памяти они занимают.

ТИПЫ ДАННЫХ В C++:

1. ЦЕЛЫЕ ЧИСЛА (Integer Types):
   
   Целые числа используются для хранения целых значений без дробной части.
   
   а) int (integer) - основной тип для целых чисел:
      - Обычно занимает 32 бита (4 байта)
      - Диапазон: от -2,147,483,648 до 2,147,483,647
      - Используется для большинства целочисленных вычислений
      - Пример: int age = 15;
   
   б) short - короткие целые числа:
      - Обычно занимает 16 бит (2 байта)
      - Диапазон: от -32,768 до 32,767
      - Используется для экономии памяти
      - Пример: short year = 2024;
   
   в) long - длинные целые числа:
      - Обычно занимает 32 или 64 бита (зависит от системы)
      - Больший диапазон, чем int
      - Пример: long population = 8000000000L;
   
   г) long long - очень длинные целые числа:
      - Обычно занимает 64 бита (8 байт)
      - Диапазон: от -9,223,372,036,854,775,808 до 9,223,372,036,854,775,807
      - Используется для очень больших чисел
      - Пример: long long bigNumber = 123456789012345LL;
   
   д) char (character) - символы:
      - Обычно занимает 8 бит (1 байт)
      - Хранит один символ
      - Может использоваться как маленькое целое число
      - Пример: char grade = 'A';
      - Важно: символы заключаются в одинарные кавычки 'A', а не "A"

2. ЧИСЛА С ПЛАВАЮЩЕЙ ТОЧКОЙ (Floating Point Types):
   
   Используются для хранения дробных чисел (с десятичной точкой).
   
   а) float - числа с плавающей точкой:
      - Обычно занимает 32 бита (4 байта)
      - Точность: около 7 десятичных знаков
      - Диапазон: от 1.2e-38 до 3.4e+38
      - Суффикс f или F указывает на float
      - Пример: float height = 1.75f;
   
   б) double - двойной точности:
      - Обычно занимает 64 бита (8 байт)
      - Точность: около 15-17 десятичных знаков
      - Диапазон: от 2.3e-308 до 1.7e+308
      - Используется по умолчанию для дробных чисел
      - Пример: double weight = 65.5;
   
   в) long double - расширенной точности:
      - Обычно занимает 80 или 128 бит
      - Максимальная точность
      - Используется для научных вычислений
      - Пример: long double pi = 3.141592653589793238L;

3. ЛОГИЧЕСКИЙ ТИП (Boolean):
   
   bool - хранит логические значения:
   - Занимает обычно 1 байт (но может быть больше)
   - Может иметь только два значения: true или false
   - Используется в условиях и логических операциях
   - Пример: bool isStudent = true;
   - При выводе: true выводится как 1, false как 0

4. БЕЗЗНАКОВЫЕ ТИПЫ (Unsigned Types):
   
   Беззнаковые типы хранят только положительные числа (и ноль).
   Удваивают диапазон положительных значений.
   
   - unsigned int - беззнаковое целое
   - unsigned short - беззнаковое короткое
   - unsigned long - беззнаковое длинное
   - unsigned char - беззнаковый символ (0-255)
   - Пример: unsigned int positiveNumber = 100;
   - Важно: unsigned int не может быть отрицательным!

5. СТРОКИ (String):
   
   string - класс для работы со строками (требует #include <string>):
   - Хранит последовательность символов
   - Динамический размер
   - Удобные операции со строками
   - Пример: string name = "Иван";
   - Важно: строки заключаются в двойные кавычки "текст"

ОБЪЯВЛЕНИЕ ПЕРЕМЕННЫХ:

Объявление переменной создает переменную, но не присваивает ей значение.
Синтаксис: тип_данных имя_переменной;

Примеры:
int age;           // объявлена, но не инициализирована (содержит "мусор")
double price;      // объявлена, но не инициализирована
char symbol;       // объявлена, но не инициализирована

Важно: Использование неинициализированной переменной - это ошибка!
Всегда инициализируйте переменные перед использованием.

ИНИЦИАЛИЗАЦИЯ ПЕРЕМЕННЫХ:

Инициализация - это присваивание начального значения при объявлении.
Синтаксис: тип_данных имя_переменной = значение;

Примеры:
int age = 15;                    // инициализация числом
double price = 99.99;            // инициализация дробным числом
char grade = 'A';                // инициализация символом
bool isActive = true;            // инициализация логическим значением
string name = "Иван";            // инициализация строкой

Альтернативные способы инициализации (C++11):
int age{15};                     // универсальная инициализация
int age(15);                     // конструктор (для простых типов)

Множественное объявление:
int a = 10, b = 20, c = 30;     // несколько переменных одного типа

ПРАВИЛА ИМЕНОВАНИЯ ПЕРЕМЕННЫХ:

1. Допустимые символы:
   - Буквы (a-z, A-Z, русские буквы)
   - Цифры (0-9)
   - Символ подчеркивания (_)
   
2. Ограничения:
   - Имя НЕ может начинаться с цифры
   - Нельзя использовать ключевые слова C++ (if, for, int и т.д.)
   - Регистр имеет значение (age и Age - разные переменные)
   
3. Стили именования:
   - camelCase: myVariable, studentAge
   - snake_case: my_variable, student_age
   - PascalCase: MyVariable (обычно для классов)
   - UPPER_CASE: MAX_SIZE (обычно для констант)
   
4. Рекомендации:
   - Используйте понятные имена (age вместо a)
   - Избегайте слишком длинных имен
   - Используйте единый стиль в проекте
   - Не используйте сокращения, если они не очевидны

Примеры правильных имен:
int studentAge = 15;
double averageScore = 4.5;
bool isPassed = true;
string studentName = "Иван";

Примеры неправильных имен:
int 2age = 15;        // начинается с цифры
int if = 10;          // ключевое слово
int a = 5;            // неинформативное имя

КОНСТАНТЫ:

Константа - это переменная, значение которой нельзя изменить после инициализации.

1. const - константы времени компиляции:
   Синтаксис: const тип_данных ИМЯ = значение;
   
   Особенности:
   - Значение должно быть известно на этапе компиляции
   - Нельзя изменить после инициализации
   - Обычно пишутся ЗАГЛАВНЫМИ буквами
   - Компилятор может оптимизировать код с константами
   
   Примеры:
   const int MAX_STUDENTS = 30;
   const double PI = 3.14159;
   const string SCHOOL_NAME = "Дворец Пионеров";
   
   Попытка изменить константу приведет к ошибке компиляции:
   const int x = 10;
   x = 20;  // ОШИБКА! Нельзя изменить константу

2. #define - макросы препроцессора:
   Синтаксис: #define ИМЯ значение
   
   Особенности:
   - Обрабатывается до компиляции
   - Простая текстовая замена
   - Не имеет типа
   - Менее безопасно, чем const
   
   Пример:
   #define MAX_SIZE 100
   #define PI 3.14159
   
   Рекомендация: Используйте const вместо #define в современном C++

ВВОД ДАННЫХ (cin):

Чтобы получить данные от пользователя, используется cin (console input):
   int age;
   cout << "Введите возраст: ";
   cin >> age;
   // cin >> - считывает значение с клавиатуры и записывает в переменную
   // >> - оператор ввода (обратите внимание: >> а не <<)

ПРЕОБРАЗОВАНИЕ ТИПОВ (Type Casting):

Иногда нужно преобразовать значение одного типа в другой.

1. Неявное преобразование (автоматическое):
   int x = 10;
   double y = x;  // int автоматически преобразуется в double

2. Явное преобразование (C-style):
   double x = 3.14;
   int y = (int)x;  // преобразование double в int (отбрасывается дробная часть)

3. Явное преобразование (C++ style):
   double x = 3.14;
   int y = static_cast<int>(x);  // более безопасный способ

Важно: При преобразовании могут теряться данные:
- double в int - теряется дробная часть
- int в char - могут теряться старшие биты
- Большой тип в маленький - может произойти переполнение

СИНТАКСИС УРОКА:

1. Объявление переменной:
   тип имя;
   int age;

2. Инициализация переменной:
   тип имя = значение;
   int age = 15;
   double price = 99.99;
   char grade = 'A';
   bool isReady = true;
   string name = "Иван";    // требует #include <string>

3. Константа:
   const тип ИМЯ = значение;
   const double PI = 3.14159;

4. Ввод с клавиатуры:
   cin >> переменная;
   // Считывает ввод пользователя и записывает в переменную

5. Вывод значения переменной:
   cout << "Текст: " << переменная << endl;

6. Определение размера типа:
   sizeof(тип)     // размер типа в байтах
   sizeof(переменная)  // размер переменной в байтах

ПРИМЕРЫ КОДА:
*/

#include <iostream>
#include <string>
using namespace std;

// === ПРИМЕРЫ ИЗ УРОКА ===
void examples() {
    // Объявление и инициализация переменных разных типов
    
    // Целые числа
    int age = 15;
    short year = 2024;
    long population = 8000000000L;
    long long bigNumber = 123456789012345LL;
    
    // Числа с плавающей точкой
    float height = 1.75f;
    double weight = 65.5;
    long double pi = 3.141592653589793238L;
    
    // Логический тип
    bool isStudent = true;
    bool isWorking = false;
    
    // Символы
    char grade = 'A';
    char symbol = '@';
    
    // Беззнаковые типы
    unsigned int positiveNumber = 100;
    unsigned char smallNumber = 255;
    
    // Константы
    const int MAX_STUDENTS = 30;
    const double PI = 3.14159;
    const string SCHOOL_NAME = "Дворец Пионеров";
    
    // Вывод значений переменных
    cout << "=== ПЕРЕМЕННЫЕ РАЗНЫХ ТИПОВ ===" << endl;
    cout << "Возраст: " << age << endl;
    cout << "Год: " << year << endl;
    cout << "Население мира: " << population << endl;
    cout << "Большое число: " << bigNumber << endl;
    
    cout << "\n=== ЧИСЛА С ПЛАВАЮЩЕЙ ТОЧКОЙ ===" << endl;
    cout << "Рост: " << height << " м" << endl;
    cout << "Вес: " << weight << " кг" << endl;
    cout << "Пи: " << pi << endl;
    
    cout << "\n=== ЛОГИЧЕСКИЕ ЗНАЧЕНИЯ ===" << endl;
    cout << "Студент: " << isStudent << endl;   // 1 = true, 0 = false
    cout << "Работает: " << isWorking << endl;
    
    cout << "\n=== СИМВОЛЫ ===" << endl;
    cout << "Оценка: " << grade << endl;
    cout << "Символ: " << symbol << endl;
    
    cout << "\n=== БЕЗЗНАКОВЫЕ ТИПЫ ===" << endl;
    cout << "Положительное число: " << positiveNumber << endl;
    cout << "Маленькое число: " << (int)smallNumber << endl;
    
    cout << "\n=== КОНСТАНТЫ ===" << endl;
    cout << "Максимум студентов: " << MAX_STUDENTS << endl;
    cout << "Пи: " << PI << endl;
    cout << "Название школы: " << SCHOOL_NAME << endl;
}

// === УПРАЖНЕНИЯ ===

// Упражнение 1: Калькулятор возраста
// Создай программу, которая вычисляет возраст в днях, часах и минутах:
void exercise1() {
    int age = 15;
    int daysInYear = 365;
    int hoursInDay = 24;
    int minutesInHour = 60;
    
    int ageInDays = age * daysInYear;
    int ageInHours = ageInDays * hoursInDay;
    int ageInMinutes = ageInHours * minutesInHour;
    
    cout << "Возраст: " << age << " лет" << endl;
    cout << "В днях: " << ageInDays << " дней" << endl;
    cout << "В часах: " << ageInHours << " часов" << endl;
    cout << "В минутах: " << ageInMinutes << " минут" << endl;
}

// Упражнение 2: Конвертер единиц
// Создай программу для конвертации единиц измерения:
void exercise2() {
    double meters = 100.0;
    double centimeters = meters * 100;
    double kilometers = meters / 1000;
    double inches = meters * 39.37;
    
    cout << "Длина: " << meters << " метров" << endl;
    cout << "В сантиметрах: " << centimeters << " см" << endl;
    cout << "В километрах: " << kilometers << " км" << endl;
    cout << "В дюймах: " << inches << " дюймов" << endl;
}

// Упражнение 3: Информация о студенте
// Создай программу с информацией о студенте:
void exercise3() {
    string name = "Иван";
    int age = 16;
    float height = 1.80f;
    double weight = 70.5;
    char grade = 'B';
    bool isScholarship = true;
    
    cout << "=== ИНФОРМАЦИЯ О СТУДЕНТЕ ===" << endl;
    cout << "Имя: " << name << endl;
    cout << "Возраст: " << age << " лет" << endl;
    cout << "Рост: " << height << " м" << endl;
    cout << "Вес: " << weight << " кг" << endl;
    cout << "Оценка: " << grade << endl;
    cout << "Стипендия: " << isScholarship << endl;  // 1 = true, 0 = false
}

// Упражнение 4: Математические константы
// Создай программу с математическими константами:
void exercise4() {
    const double PI = 3.14159265359;
    const double E = 2.71828182846;
    const double GOLDEN_RATIO = 1.61803398875;
    const int SPEED_OF_LIGHT = 299792458; // м/с
    
    cout << "=== МАТЕМАТИЧЕСКИЕ КОНСТАНТЫ ===" << endl;
    cout << "Пи: " << PI << endl;
    cout << "Число Эйлера: " << E << endl;
    cout << "Золотое сечение: " << GOLDEN_RATIO << endl;
    cout << "Скорость света: " << SPEED_OF_LIGHT << " м/с" << endl;
}

/*
ДОМАШНИЕ ЗАДАНИЯ:

Задание 1: Программа-анкета
Создай программу-анкету с твоими данными:
- Имя (string)
- Возраст (int)
- Рост (float)
- Вес (double)
- Любимый цвет (string)
- Любимое число (int)

Задание 2: Калькулятор площади
Создай программу для вычисления площади различных фигур:
- Квадрат (сторона)
- Прямоугольник (длина, ширина)
- Треугольник (основание, высота)

Задание 3: Конвертер валют
Создай программу для конвертации валют:
- Рубли в доллары
- Доллары в рубли
- Рубли в евро
- Евро в рубли

ПРОВЕРОЧНЫЕ ВОПРОСЫ:

1. Какие основные типы данных есть в C++?
2. В чем разница между int и float?
3. Что такое константа и зачем она нужна?
4. Какие правила именования переменных?
5. В чем разница между объявлением и инициализацией?
6. Что такое беззнаковые типы?
7. Какой тип данных использовать для хранения символа?

ЧТО ДАЛЬШЕ:
На следующем уроке мы изучим:
- Арифметические операторы
- Операторы сравнения
- Логические операторы
- Приоритет операций

ВРЕМЯ ИЗУЧЕНИЯ: 50-60 минут
ВРЕМЯ ПРАКТИКИ: 40-50 минут
ОБЩЕЕ ВРЕМЯ: 1.5-2 часа

===========================================
*/

// === ГЛАВНАЯ ФУНКЦИЯ ===
int main() {
    cout << "=== УРОК 2: ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ ===" << endl;
    
    // Примеры из урока
    examples();
    
    // Упражнения
    cout << "\n=== УПРАЖНЕНИЕ 1: КАЛЬКУЛЯТОР ВОЗРАСТА ===" << endl;
    exercise1();
    
    cout << "\n=== УПРАЖНЕНИЕ 2: КОНВЕРТЕР ЕДИНИЦ ===" << endl;
    exercise2();
    
    cout << "\n=== УПРАЖНЕНИЕ 3: ИНФОРМАЦИЯ О СТУДЕНТЕ ===" << endl;
    exercise3();
    
    cout << "\n=== УПРАЖНЕНИЕ 4: МАТЕМАТИЧЕСКИЕ КОНСТАНТЫ ===" << endl;
    exercise4();
    
    return 0;
}
























