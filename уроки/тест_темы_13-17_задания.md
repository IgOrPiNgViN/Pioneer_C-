# Тест по темам уроков 13–17 (тест с выбором ответа)

**Темы:** 13 — файлы; 14 — классы и ООП; 15 — конструкторы и деструкторы; 16 — наследование; 17 — полиморфизм.

В каждом варианте **15 вопросов**. У каждого вопроса **ровно один верный** вариант — **A**, **B**, **C** или **D**. Порядок вариантов **перемешан** (верный ответ не привязан к одной букве).

Ответы — в файле `тест_темы_13-17_ответы.md`.

---

## Вариант 1

**1.** (Урок 13) Какой заголовок подключают для `std::ifstream` и `std::ofstream`?

- **A)** `#include <iostream>`  
- **B)** `#include <file>`  
- **C)** `#include <fstream>`  
- **D)** `#include <stdio.h>`  

**2.** (Урок 13) Режим `std::ios::app` при открытии файла на запись означает:

- **A)** открыть только для чтения  
- **B)** удалить файл перед записью  
- **C)** записывать в начало файла, сдвигая старые байты  
- **D)** дописывать данные в конец файла, не затирая старое содержимое  

**3.** (Урок 13) Проверка `file.is_open()` после `open()` нужна, чтобы:

- **A)** узнать, успешно ли открыт файл, и не работать с несуществующим потоком  
- **B)** ускорить чтение файла  
- **C)** закрыть файл автоматически  
- **D)** перевести поток в бинарный режим  

**4.** (Урок 14) **Объект** в C++ — это:

- **A)** ключевое слово языка  
- **B)** только функция внутри класса  
- **C)** конкретный экземпляр класса (переменная типа класса)  
- **D)** то же самое, что `struct`, но без методов  

**5.** (Урок 14) **Инкапсуляция** — это прежде всего:

- **A)** наследование двух классов друг от друга  
- **B)** сокрытие внутренних данных и контроль доступа через интерфейс класса  
- **C)** использование только глобальных переменных  
- **D)** обязательное объявление всех полей как `public`  

**6.** (Урок 14) Поля делают `private`, а доступ организуют через геттеры и сеттеры, чтобы:

- **A)** контролировать и проверять изменение данных извне  
- **B)** программа компилировалась быстрее  
- **C)** нельзя было хранить числа в полях  
- **D)** методы занимали меньше памяти  

**7.** (Урок 14) В записи `int getX() const;` слово `const` означает, что:

- **A)** метод возвращает константу времени компиляции  
- **B)** поле `x` нельзя читать  
- **C)** объект класса становится неизменяемым навсегда  
- **D)** метод не должен изменять логическое состояние объекта (такой метод можно вызывать у `const`-объектов)  

**8.** (Урок 15) **Конструктор по умолчанию** — это конструктор:

- **A)** который всегда с одним параметром `int`  
- **B)** который вызывается только при `delete`  
- **C)** без параметров или со всеми параметрами, имеющими значения по умолчанию  
- **D)** который нельзя определить вручную  

**9.** (Урок 15) Список инициализации в `Point(int a, int b) : x(a), y(b) { }` нужен, чтобы:

- **A)** объявить новые локальные переменные `x` и `y`  
- **B)** инициализировать поля `x` и `y` до входа в тело конструктора  
- **C)** удалить поля класса  
- **D)** сделать поля статическими  

**10.** (Урок 15) **Деструктор** вызывается:

- **A)** при уничтожении объекта (конец области видимости, `delete` и т.п.)  
- **B)** только при явном вызове из `main` по имени класса  
- **C)** один раз за всю программу при старте  
- **D)** перед каждым вызовом конструктора  

**11.** (Урок 16) Запись `class Dog : public Animal` означает:

- **A)** `Dog` — дружественный класс для `Animal`  
- **B)** `Animal` вложен в `Dog`  
- **C)** это объявление двух независимых классов  
- **D)** `Dog` наследует `Animal` публично  

**12.** (Урок 16) Из функции `main` к **protected**-полю базового класса у объекта производного класса:

- **A)** доступ есть напрямую, как к `public`  
- **B)** доступ есть только через `friend` в `main`  
- **C)** прямого доступа нет; поле видно в методах класса и наследников  
- **D)** доступ есть, если базовый класс пустой  

**13.** (Урок 16) В базовом классе метод `void f();` **не** помечен как `virtual`. В производном объявлен `void f();`. Вызов `Base* p = &derived; p->f();` :

- **A)** вызовет версию `f` из базового класса  
- **B)** вызовет версию `f` из производного класса  
- **C)** вызовет обе версии по очереди  
- **D)** всегда приведёт к ошибке времени выполнения  

**14.** (Урок 17) Ключевое слово `virtual` у метода базового класса нужно, чтобы:

- **A)** метод нельзя было переопределить в наследнике  
- **B)** метод выполнялся быстрее  
- **C)** поля базового класса стали `public`  
- **D)** при вызове через указатель или ссылку на базовый тип вызывалась версия фактического типа объекта  

**15.** (Урок 17) **Чисто виртуальная** функция (`virtual void foo() = 0;`) означает:

- **A)** функция всегда возвращает ноль  
- **B)** в базовом классе нет реализации; класс абстрактный, объект такого класса создать нельзя  
- **C)** функция вызывается только один раз за время работы программы  
- **D)** это обычный статический метод базового класса  

---

## Вариант 2

**1.** (Урок 13) Класс потока для **чтения** из файла в C++:

- **A)** `std::ifstream`  
- **B)** `std::ofstream`  
- **C)** `std::cout`  
- **D)** `std::clog`  

**2.** (Урок 13) Вызов `close()` у файлового потока:

- **A)** удаляет файл с диска  
- **B)** обязателен, иначе компилятор выдаст ошибку  
- **C)** закрывает файл; деструктор потока тоже закроет файл, но `close()` позволяет освободить ресурс раньше  
- **D)** работает только для `std::cin`  

**3.** (Урок 13) После `getline(cin, s)` следующий `cin >> number` часто ведёт себя неправильно, потому что:

- **A)** `getline` навсегда блокирует поток  
- **B)** в буфере может остаться символ новой строки; часто вызывают `ignore()`  
- **C)** после строки числа из потока читать запрещено стандартом  
- **D)** нужно писать `number = stoi(s)` вместо `cin`  

**4.** (Урок 14) У объявления `class` члены по умолчанию имеют доступ:

- **A)** `public`  
- **B)** `protected`  
- **C)** `private`  
- **D)** `internal`  

**5.** (Урок 14) По сравнению с публичным геттером без сеттера, **сильнее** ломает границу инкапсуляции (открывает `private` постороннему коду):

- **A)** объявление `friend`-функции или `friend class`  
- **B)** метод `public: void print() const;`  
- **C)** константный геттер `const string& getName() const;`  
- **D)** перегрузка оператора `<<` только для `public` полей  

**6.** (Урок 14) Корректная сигнатура **константного геттера** для поля `std::string name`:

- **A)** `void getName() const;`  
- **B)** `const std::string& getName() const;`  
- **C)** `std::string& getName();`  
- **D)** `static std::string getName();`  

**7.** (Урок 14) **Инвариант класса** — это:

- **A)** количество байт, которое занимает объект  
- **B)** условие, которое должно выполняться для корректного объекта (например, день месяца в допустимых пределах)  
- **C)** обязательное наличие виртуального деструктора  
- **D)** запрет на использование конструкторов  

**8.** (Урок 15) Типичный вид **конструктора копирования** для класса `T`:

- **A)** `void T(const T& other);`  
- **B)** `T(int copyId);`  
- **C)** `static T copy();`  
- **D)** `T(const T& other);`  

**9.** (Урок 15) Несколько конструкторов в одном классе — это:

- **A)** перегрузка; компилятор выбирает конструктор по аргументам  
- **B)** ошибка компиляции  
- **C)** допустимо только у вложенных классов  
- **D)** называется множественным наследованием  

**10.** (Урок 15) Деструктор класса:

- **A)** может иметь параметры по умолчанию  
- **B)** не имеет параметров  
- **C)** должен возвращать `void` явно в объявлении  
- **D)** объявляется как `ClassName::~ClassName(int n)`  

**11.** (Урок 16) При `class B : public A` **публичные** члены класса `A` в классе `B` становятся:

- **A)** `public`  
- **B)** `private`  
- **C)** `protected`  
- **D)** без модификатора доступа  

**12.** (Урок 16) Производный класс **напрямую** (из своего метода) к **private**-полю базового класса:

- **A)** может обращаться свободно  
- **B)** может, если в базе есть конструктор по умолчанию  
- **C)** может только в статическом методе производного класса  
- **D)** не может; нужны методы базы, `protected` в базе или иной предусмотренный дизайн  

**13.** (Урок 16) **Виртуальное наследование** при «ромбе» (общий предок по двум веткам) позволяет:

- **A)** отключить полиморфизм  
- **B)** убрать необходимость в деструкторе  
- **C)** сделать все методы базы `static`  
- **D)** иметь один экземпляр общего виртуального базового подобъекта вместо дублирования  

**14.** (Урок 17) Виртуальный метод, вызванный через **указатель на базовый класс**, выполняется:

- **A)** всегда как метод базового класса (статическое связывание)  
- **B)** только если указатель равен `nullptr`  
- **C)** в варианте, соответствующем **фактическому** типу объекта  
- **D)** случайно из таблицы всех функций программы  

**15.** (Урок 17) Спецификатор `override` у метода в производном классе:

- **A)** заменяет `virtual` в базовом классе  
- **B)** запрещает наследникам дальнейшее переопределение  
- **C)** включает проверку компилятором, что метод переопределяет виртуальный метод базы с подходящей сигнатурой  
- **D)** используется только для чисто виртуальных функций в базе  

---

*Конец заданий.*
