# Итоговая контрольная работа по C++ (Уроки 1–16)
## Вариант 4

**Время выполнения:** 90 минут  
**Максимальный балл:** 100 баллов

**Формат ответов:** теоретические вопросы — **развёрнуто** (3–8 предложений, при необходимости пример кода). Практические — краткий ответ и краткое обоснование.

---

## Теоретическая часть (30 баллов)

**Вопрос 1 (6 баллов)** — *Урок 5. Циклы + Урок 3. Операторы*  
Каков порядок выполнения операций в выражении `a + b * c - d / e`? Что такое короткое замыкание у `&&` и `||`? Приведите пример выражения, где результат зависит от приоритета операторов.

---

**Вопрос 2 (6 баллов)** — *Урок 7. Строки + Урок 4. Условия*  
Как проверить, пуста ли строка `std::string`? Как прочитать строку с пробелами через `getline`? Почему после `cin >> число` перед `getline` иногда «пропадает» ввод и что с этим делать?

---

**Вопрос 3 (6 баллов)** — *Урок 11. Работа с файлами*  
Для чего используются классы `ifstream` и `ofstream`? Что проверяет метод `is_open()`? Чем режим `ios::app` отличается от обычной записи? Почему поток нужно закрывать (`close()`)?

---

**Вопрос 4 (6 баллов)** — *Урок 12. Классы и ООП*  
Что такое класс, объект, поле и метод? Объясните инкапсуляцию и модификаторы `public`, `private`, `protected`. Зачем поля часто делают `private`, а доступ — через методы?

---

**Вопрос 5 (6 баллов)** — *Урок 16. Перегрузка операторов + Урок 12. Классы*  
Можно ли перегрузить оператор вывода `<<` для своего класса? Должен ли такой оператор быть дружественной функцией (`friend`)? Объясните, почему `cout << obj` удобнее, чем отдельный метод `print()`.

---

## Практическая часть (70 баллов)

**Задание 1 (14 баллов)** — *Урок 1. Введение + Урок 2. Переменные*  
Найдите **все ошибки** в программе, исправьте и укажите корректный вывод.

```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 5
    int b = 2.5;
    bool flag = 1;
    cout << a / b << endl;
    cout << flag << endl;
    return 0;
}
```

---

**Задание 2 (14 баллов)** — *Урок 4. Условия*  
Что выведет код?

```cpp
int n = 14;
int res = 0;
if (n % 2 == 0) {
    if (n > 10) {
        res = n / 2 + 3;
    } else {
        res = n + 1;
    }
} else {
    res = n - 1;
}
cout << res;
```

**Ответ:** ___________

---

**Задание 3 (14 баллов)** — *Урок 9. Структуры*  
Допишите **одну строку** вместо `// ___`, чтобы программа вывела `Иванов 85`.

```cpp
#include <iostream>
using namespace std;

struct Score {
    string name;
    int points;
};

int main() {
    Score s = {"Иванов", 80};
    // ___
    cout << s.name << " " << s.points << endl;
    return 0;
}
```

**Ваша строка:** ___________

---

**Задание 4 (14 баллов)** — *Урок 10. Функции*  
В рекурсивной функции **ошибка** (бесконечная рекурсия). Найдите её, исправьте и укажите правильный вывод для `factorial(4)`.

```cpp
#include <iostream>
using namespace std;

int factorial(int n) {
    if (n == 0)
        return 1;
    return n * factorial(n);
}

int main() {
    cout << factorial(4);
    return 0;
}
```

---

**Задание 5 (14 баллов)** — *Урок 14. Наследование*  
Что выведет программа? Объясните, какие методы вызываются.

```cpp
#include <iostream>
using namespace std;

class Device {
public:
    void info() {
        cout << "Device ";
    }
};

class Phone : public Device {
public:
    void info() {
        cout << "Phone ";
    }
};

int main() {
    Phone p;
    Device *d = &p;
    p.info();
    d->info();
    return 0;
}
```

**Ответ:** ___________

---

## Критерии оценивания

| Баллы | Оценка |
|-------|--------|
| 85–100 | 5 (отлично) |
| 70–84 | 4 (хорошо) |
| 50–69 | 3 (удовлетворительно) |
| 0–49 | 2 (неудовлетворительно) |
