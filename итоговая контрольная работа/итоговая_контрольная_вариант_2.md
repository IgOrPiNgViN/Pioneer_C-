# Итоговая контрольная работа по C++ (Уроки 1–16)
## Вариант 2

**Время выполнения:** 90 минут  
**Максимальный балл:** 100 баллов

**Формат ответов:** теоретические вопросы — **развёрнуто** (3–8 предложений, при необходимости пример кода). Практические — краткий ответ и краткое обоснование.

---

## Теоретическая часть (30 баллов)

**Вопрос 1 (6 баллов)** — *Урок 2. Переменные и типы данных*  
Перечислите основные целочисленные и вещественные типы C++ (`int`, `long`, `float`, `double`, `char`, `bool`). Что такое явное и неявное приведение типов? Что произойдёт при присваивании `int x = 7.89;`?

---

**Вопрос 2 (6 баллов)** — *Урок 7. Строки*  
Чем `std::string` удобнее C-строки (`char[]`)? Как получить длину строки, обратиться к символу по индексу, объединить две строки и сравнить их? Приведите короткие примеры кода.

---

**Вопрос 3 (6 баллов)** — *Урок 8. Указатели*  
Дайте определение указателя. Для чего используются операторы `&` и `*`? Что такое `nullptr` и почему опасно разыменовывать «висячий» указатель? Чем указатель отличается от ссылки (`int &r`)?

---

**Вопрос 4 (6 баллов)** — *Урок 13. Конструкторы и деструктор*  
Что такое конструктор и деструктор класса? Чем конструктор по умолчанию отличается от параметризованного? Что такое список инициализации членов в конструкторе и зачем он нужен?

---

**Вопрос 5 (6 баллов)** — *Урок 16. Перегрузка операторов*  
Что значит «перегрузить оператор» для пользовательского класса? Какие операторы чаще всего перегружают? Можно ли перегрузить оператор `[]` для доступа к элементам? Приведите пример объявления перегрузки `operator+`.

---

## Практическая часть (70 баллов)

**Задание 1 (14 баллов)** — *Урок 3. Операторы*  
Вычислите значение `x` после выполнения кода.

```cpp
int x = 10;
int y = 4;
x = x / y + y % 3;
x += (y > 3 ? 2 : 5);
x *= 2;
x -= 1;
```

**Ответ:** ___________

---

**Задание 2 (14 баллов)** — *Урок 6. Массивы*  
Что выведет программа?

```cpp
int data[4] = {6, 2, 9, 2};
int s = 0;
for (int i = 0; i < 4; i++) {
    if (i % 2 == 0) {
        s += data[i];
    } else {
        s -= data[i] / 2;
    }
}
cout << s;
```

**Ответ:** ___________

---

**Задание 3 (14 баллов)** — *Урок 9. Структуры + Урок 10. Функции*  
Найдите ошибки, исправьте код и укажите, что выведется после исправления.

```cpp
struct Point {
    int x, y;
};

int sum(Point p) {
    return p.x + p.y;
}

Point makePoint(int a, int b) {
    Point p;
    p.x = a;
    p.y = b;
}

int main() {
    Point p1 = makePoint(3, 4);
    cout << sum(p1);
    return 0;
}
```

---

**Задание 4 (14 баллов)** — *Урок 7. Строки*  
Что выведет программа? Объясните работу `length`, `+=` и сравнения символов.

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string a = "ab";
    a += "cd";
    cout << a.length();
    cout << (a[3] == 'd');
    return 0;
}
```

**Ответ:** ___________

---

**Задание 5 (14 баллов)** — *Урок 15. Полиморфизм*  
Что выведет код? Объясните роль `virtual` и позднего связывания.

```cpp
class Animal {
public:
    virtual void speak() {
        cout << "Animal ";
    }
};

class Dog : public Animal {
public:
    void speak() override {
        cout << "Dog ";
    }
};

class Cat : public Animal {
public:
    void speak() override {
        cout << "Cat ";
    }
};

int main() {
    Animal *a1 = new Dog();
    Animal *a2 = new Cat();
    a1->speak();
    a2->speak();
    delete a1;
    delete a2;
    return 0;
}
```

**Ответ:** ___________

---

## Критерии оценивания

| Баллы | Оценка |
|-------|--------|
| 85–100 | 5 (отлично) |
| 70–84 | 4 (хорошо) |
| 50–69 | 3 (удовлетворительно) |
| 0–49 | 2 (неудовлетворительно) |
